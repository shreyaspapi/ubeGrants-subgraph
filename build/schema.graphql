type GrantMilestoneDelivery @entity {
  id: ID!
  ipfsHash: String!
  approved: Boolean!
  time: BigInt!
}

type Grant @entity {
  id: ID!
  grantId: BigInt!
  state: BigInt
  ipfs: String
  grantee: Bytes
  milestoneAmounts: [BigInt!] # uint256[]
  milestoneDeliveries: [GrantMilestoneDelivery]!
  nextPayout: BigInt!
  time: BigInt!
  block: BigInt!
}

type ContractOwner @entity (immutable: true) {
  id: ID!
  contract: Bytes!
  owner: Bytes!
  time: BigInt!
  block: BigInt!
}
